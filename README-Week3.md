For this code, I used the three main codes to do a simple addition of bills. 

(string bill;) adds the subject "bill" to carry a string of characters and numbers

(int price) adds the subject "price" to carry intergers only

(float cost) adds the subject "cost" to carry integers with decimals

## (int) can only carry whole numbers vs (float) which can do decimals

Finally printing the sentence, these subject are called upon first to be added values in Line 8 through 17 of my code. 

So when recalled in line 19, the values entered to the subject can be used.
